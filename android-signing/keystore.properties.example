# Copy to android/keystore.properties (never commit the real file).
# storeFile is relative to android/app/
storeFile=release.keystore
storePassword=CHANGE_ME
keyAlias=kavita-reader
keyPassword=CHANGE_ME
